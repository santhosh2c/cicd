_schema-version: 3.3.0
ID: cicd
description: A simple CAP project.
version: 1.0.0
modules:
  - name: cicd-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: cicd-auth
      - name: cicd-db
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: cicd-destination-content
    type: com.sap.application.content
    requires:
      - name: cicd-destination-service
        parameters:
          content-target: true
      - name: cicd_html_repo_host
        parameters:
          service-key:
            name: cicd_html_repo_host-key
      - name: cicd-auth
        parameters:
          service-key:
            name: cicd-auth-key
    parameters:
      content:
        instance:
          destinations:
            - Name: cicd_router_cicd_html_repo_host
              ServiceInstanceName: cicd-html5-app-host-service
              ServiceKeyName: cicd_html_repo_host-key
              sap.cloud.service: cicd-router
            - Authentication: OAuth2UserTokenExchange
              Name: cicd_router_cicd_auth
              ServiceInstanceName: cicd-auth
              ServiceKeyName: cicd-auth-key
              sap.cloud.service: cicd-router
          existing_destinations_policy: ignore
    build-parameters:
      no-source: true
  - name: cicd-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: cicd_html_repo_host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - project1.zip
          name: project1
          target-path: resources/
  - name: project1
    type: html5
    path: app/project1
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []

  - name: cicd-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: cicd-db
resources:
  - name: cicd-auth
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        tenant-mode: dedicated
        xsappname: cicd-${org}-${space}
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
  - name: cicd-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
              - Authentication: NoAuthentication
                HTML5.DynamicDestination: true
                HTML5.ForwardAuthToken: true
                Name: srv-api
                ProxyType: Internet
                Type: HTTP
                URL: ~{srv-api/srv-url}
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: cicd-destination-service
      service-plan: lite
    requires:
      - name: srv-api
  - name: cicd_html_repo_host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: cicd-html5-app-host-service
      service-plan: app-host
  - name: cicd-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
